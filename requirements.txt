# OyNIx Browser - Dependencies
# Install: pip install -r requirements.txt

# Core Browser Engine
PyQt6>=6.5.0
PyQt6-WebEngine>=6.5.0

# Local LLM (auto-downloads models, runs on CPU - no GPU needed)
llama-cpp-python>=0.2.0
huggingface-hub>=0.20.0

# Search & Indexing
whoosh>=2.7.4
beautifulsoup4>=4.12.0
requests>=2.31.0

# Database
# (SQLite3 is built into Python)

# Utilities
Pillow>=10.0.0
